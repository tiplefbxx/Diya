import { useState } from "react"

const useTurn = () => {
    let [side, setSide] = useState('front')
    let activeFrontStyle = 'front-active'
    let activeBackStyle = 'back-active'

    if(side === 'front') {
        
    }
}